-
    name: 'Run install'
    hosts: all
    tasks:
        -
            name: 'Install packages'
            yum:
              name: httpd
              state: present
              update_cache: true
